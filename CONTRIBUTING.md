# Contributing

If you have a PRIMER variant with different I/O mapping or a different timekeeper chip,
open an issue / PR with:

- Your memory map (ROM/RAM ranges)
- Where the RTC register window appears
- Any differences in display services
- A short “what works / what doesn’t” description
